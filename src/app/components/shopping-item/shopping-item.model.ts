export interface shoppingItemInterface {
    name: string,
    cost: string,
    amount: string,
    comment: string,
    status: string
}

export interface itemsCount {
    active: number, 
    completed: number
}